import { Group, Text, useMantineTheme } from "@mantine/core";

export const Copyright = (): JSX.Element => {
  const { fontSizes, other } = useMantineTheme();
  return (
    <Group spacing="xs" position="center">
      footer
    </Group>
  );
};
